# 高考志愿智能推荐系统 — 版本报告

**版本**：v6.2（在 v6.1 基础上的并列 / 确定性 / 中性选项修订）
**日期**：2026-09-13
**引擎**：`funnel_engine.py`（三层漏斗 v6.0 核心 + v6.1 + 本版修订）
**状态**：回归测试 13 组 67 断言 **全部通过**；`test_funnel.py` 3/3 通过

> 本报告按**代码实际实现**撰写，不依赖 `generate_report.py`（该脚本仍描述已废弃的
> 「Jaccard 指数」产业匹配，**不可直接用于对外说明**）。

---

## 一、本版起因：用户实测反馈

用户自测时报告：**推荐的前三名是「电子信息类 / 自动化类 / 计算机类」，三者的匹配系数完全一样**。

用户的猜测是「`labeled_majors_with_volume` 里很多专业的相关词差距太小」。
**这个判断方向是对的，但低估了严重程度**——实测不是「差距太小」，
而是**输入相同 → 输出必然相同**，且伴随一个更严重的问题（见 2.2）。

---

## 二、病因诊断

### 2.1 契合度是标签集合的纯函数

`industry_match = sqrt(best_ratio × mean_ratio)`，其中 `ratios` 由专业类的 `industry_map`
标签逐个查用户向量得到。**它不包含任何专业自身的区分信息**，只取决于该专业类的标签集合。

因此：**两个专业类的标签集合只要相同，契合度必然完全相等，且对任何用户都相等。**

用户看到的那个三元组可以直接机械复现：用户向量集中在 `{互联网/软件, 人工智能/大模型, 智能制造/机器人}`
时，三个专业类的标签集合恰好一致，结果都是 **0.7746**，且顺序就是 JSON 文件里的键顺序
（电子信息类 67 / 自动化类 79 / 计算机类 85）。

### 2.2 并列会静默改变名单（比「分差小」严重得多）

Python 的 `sorted` 是稳定排序，并列时按**插入顺序**——也就是 `layer2_categories.json`
的键顺序——决定谁进 Top8。

这意味着：**数据文件的键顺序一旦变化，推荐名单就会变**，而且不会有任何提示。
实测（600 份答卷，打乱 JSON 键顺序后重跑）：

| | 改动前 | 改动后 |
|---|---|---|
| Top8 名单发生变化的答卷 | **108 / 600 = 18%** | **0 / 600** |

18% 的答卷会**换掉整张榜单**——不是顺序变，是有专业类进榜、有专业类出榜。
键顺序是「谁当初先被写进 JSON」决定的，与算法无关，等于 18% 的用户被一个与算法
无关的因素决定了推荐结果。

### 2.3 并列的规模

扫描 2000 份答卷：

| 现象 | 占比 |
|---|---|
| Top8 内存在显示级并列（四舍五入后同分） | **42.6%** |
| Top3 内存在并列（即用户看到的那种） | **7.8%** |
| 第 8 名与第 9 名同分（意味着「谁进榜」本身是随机的） | 4.3% |

并列的来源拆开看（477 组）：

| 来源 | 组数 | 占比 |
|---|---|---|
| **标签集合完全相同的专业类**（共 19 个类、8 组） | 403 | **84%** |
| 标签不同、但对这个用户恰好算出同一个数 | 74 | 16% |

最大的那一组：`医学技术类 / 中西医结合类 / 临床医学类 / 口腔医学类 / 护理学类`
标签**全都是** `['医疗健康/临床']`。一个医学意向用户的 Top8 里，这五个类永远并列满分，
它们之间的先后**完全由 JSON 键顺序决定**。

**结论**：这不是打分公式没调好，是**标签粒度不足以区分这些专业类**。
用户「相关词差距太小」的判断是对的；准确说法是「这些专业类的相关词**一模一样**」。

---

## 三、本版改动

### 改动一：排序确定性（②a）

新增 `rank_by_score()`，把「并列时怎么排」从「JSON 键顺序」改成「**用第二把尺子**」：

```python
def rank_by_score(items, *, score_key, name_key, fit_key=None):
    # 第一趟：按名称升序排——先消灭数据文件的键顺序影响
    ordered = sorted(items, key=lambda x: str(x.get(name_key, "")))
    # 第二趟：稳定排序按主分降序，并列者保持上一趟的名称序
    if fit_key:
        ordered.sort(key=lambda x: (x.get(score_key, 0.0), x.get(fit_key, 0.0)), reverse=True)
    else:
        ordered.sort(key=lambda x: x.get(score_key, 0.0), reverse=True)
    return ordered
```

两个要点：

1. **必须先按名称排一趟**。直接在原列表上做稳定排序，并列者仍然保持原来的插入顺序，
   等于没改。第一趟把顺序变成「与文件无关的确定顺序」，第二趟才谈得上稳定。
2. **并列时比第二把尺子**。L1 用 `cognitive_sim`，L2 用 `industry_match`，L3 用 `micro_match`。
   主分相同时，契合度更高的排前面——这是有意义的信息，不是随意断平。
   两把尺子都相等时，才落到名称序（至少是确定的、可复现的）。

`_run_funnel_impl` 里两处排序也一并替换（L2 的 `fit_key="industry_match"`、
L3 补位用的 `score_key="category_score"`）。

### 改动二：并列如实披露（②b）

即使排了序，**并列本身仍是事实**，UI 上不该假装它们有先后。新增：

```python
def _annotate_fit_ties(output):
    groups = {}
    for c in output:
        groups.setdefault(round(c.get("fit_score", 0.0), 3), []).append(c)
    for cats in groups.values():
        if len(cats) < 2:
            continue
        for c in cats:
            c["fit_tied_with"] = [o["category_name"] for o in cats if o is not c]
```

卡片上渲染为（契合度数字下方一行浅灰小字）：

> 与 自动化类、计算机类 并列——你们在你没表达过意向的维度上才有区别

**为什么后面还要跟一句解释**：并列在大多数情况下**不是缺陷**，而是真实的算法结论——
这几个专业类对这位用户确实是等价的。但用户会怀疑「是不是没算出来」。如实说明
并列的原因（区别只在未申报维度上），比让它看起来像 bug 要好。

### 改动三：M1–M10 补「都不感兴趣」选项（③①）

**病因**（v6.1 报告 6.1 已记录）：M1–M10 每题都注入产业权重，一个对主流方向无感的用户
会因为反复选「稳定/理论/规律」类选项而**被动累积** `医疗健康/临床`（各题 0.1~0.15）
与 `政府/公共服务`，最后峰值落在医学——而他从未表达过医学意向。

**做法**：M1–M10 每题新增一个零权重中性选项（`neutral: True`，`industry_weights` 与
`value_weights` 均为空），文案各题贴合语境，如：

- M1「没想过，过得踏实就行」
- M2「休息、打游戏、陪家人，没什么计划」
- M4「都不算，我觉得工作稳当就好」
- M9（用 F）「说不好，上面这些都没有特别想选的」

**计分处理**（`score_macro_questions`）：选中性选项时，**该题的产业/价值权重按
「其余非中性选项的平均」注入，而不是跳过**：

```python
if opt.get("neutral"):
    non_neutral = [o["value_weights"] for o in options if not o.get("neutral")]
    if non_neutral:
        for val in VALUE_DIMENSIONS:
            value_raw[val] += sum(w.get(val, 0.0) for w in non_neutral) / len(non_neutral)
    continue
```

**这一步是关键**。如果直接 `continue` 跳过，选中性选项的答卷**总权重会变小**，
分母不变 → 所有维度的分数被系统性压低，用户会因为「诚实地说不知道」而被整体扣分。
注入均值保持了**分母不变**（中性选项对每题贡献 = 该题的平均水平，不抬高也不压低），
同时**消除了任意一个具体方向的累积**。

**验证**：

| 检查 | 结果 |
|---|---|
| 不选中性选项的答卷，分数与加选项之前**逐维度相同** | 120 份答卷全部一致（分母不变） |
| 全选中性选项 → 产业向量**恒为 0** | 峰值 0.0，非零维数 0（医学不再被动累积） |
| 全选中性选项 → 风险容忍度落在**中档** | 42.5（`low` 阈值 35.0） |

最后一项：改之前全中性答卷的风险容忍度会被判成 `low`——因为「稳定」类选项的
价值权重被反复累加，这同样是没表达意向却被系统性判定。

**M11 没有加中性选项**：它的 G「以上都不是，我更想去前面那些主流方向」本身就是
「这些方向我都不选」的表达（产业权重为空），语义已覆盖。

### 改动四：未申报出路打折（③③）

**病因**（v6.1 报告 6.3 已记录）：`industry_match = sqrt(best × mean)` 的 `mean` 项
会因「专业类挂着用户没点的标签」而扣分。而**窄口径的明确申报恰恰表现为
「只有一条标签对得上」**——这正好惩罚了最明确的表达。

实测：`物流管理与工程类` 标签 `{物流/供应链, 零售/电商/消费, 智能制造/机器人}`，
用户明确申报物流 1.0、零售 0.35、没提过智能制造 → `sqrt(1.0 × 0.45) = 0.67` 落败，
而 `医学技术类` 标签全是医疗 → `mean ≈ 1.0` 胜出。

**`mean` 项不能删**：它是防「标签广度刷分」和提供区分度的关键（删掉后
全 `max` 会让大量专业类并列 1.000，v6.1 已实测）。

**做法**：把等权均值换成**对未申报维度弱惩罚的加权均值**：

```python
UNDECLARED_EXIT_WEIGHT = 0.5

def _weighted_mean_ratio(ratios):
    declared   = sum(1 for r in ratios if r > 0)      # 用户申报过、且该专业类也有的出路
    undeclared = len(ratios) - declared
    denom = declared + UNDECLARED_EXIT_WEIGHT * undeclared
    return sum(ratios) / denom if denom > 0 else 0.0
```

语义：**「用户没提过这个方向」（未申报）比「用户提了、但不搭」（申报了但比值为 0）
扣分更少**。前者是无信息，后者是负面信息。

**β = 0.5 是扫出来的，不是拍的**。在 0.0 / 0.2 / 0.3 / 0.5 / 0.7 / 1.0 上各跑 400 份
同一批答卷：

| β | 不同分值 | 极差 | 零契合 | 并列答卷 | 截断线并列 | 主流#1 |
|---|---|---|---|---|---|---|
| 0.0（极端） | 低 | — | — | — | — | 不达标 |
| 0.3 | — | — | — | — | — | **技术#2 翻成生物医学工程类、金融#1 翻成经济学类** |
| **0.5** | **7.513** | **0.3969** | 0.0% | **39.5%** | 4.0% | 四类主流全部保持 |
| 1.0（=旧等权） | 7.500 | 0.4071 | 0.0% | 41.5% | 3.8% | 四类主流全部保持 |

**β=0.3 被否掉的理由**：它把「技术画像」的第二名翻成生物医学工程类、把「金融画像」的
第一名从统计学类翻成经济学类——**主流画像的头部推荐被改动了**，而代价只是不同分值
多涨了约 1.9 个百分点。收益远小于代价，所以取 0.5（保留收益、不动主流头部）。

400 份答卷 A/B 对照：Top8 平均重合度 **90.9%**（400 份中 94 份完全一致），
即改动**没有全局重排**，只在「窄口径申报」的场景上生效——这正是预期的作用域。

定向场景改善（60 份/场景）：

| 场景 | 改动前 | 改动后 |
|---|---|---|
| 技术倾向用户 + 明确申报物流 | 交通运输类 2/60 | **15/60** |
| 技术倾向用户 + 明确申报体育 | 体育学类 32/60 | **38/60** |

---

## 四、实测效果汇总

| 指标 | 改动前 | 改动后 |
|---|---|---|
| 打乱数据文件键顺序后 Top8 名单变化的答卷 | 108 / 600 = **18%** | **0 / 600** |
| 不同分值（满分 8） | 7.500 | 7.513 |
| 契合度极差 | 0.4071 | 0.3969 |
| 契合度为 0 的专业类 | 0.0% | 0.0% |
| Top8 存在并列的答卷 | 41.5% | 39.5% |
| 第 8/9 名同分（截断线并列） | 3.8% | 4.0% |
| 全中性答卷的产业向量峰值 | 被动累积到医学 | **0.0** |
| 全中性答卷的风险容忍度 | 被判为 `low` | **42.5（中档）** |

---

## 五、已知遗留问题（**未改，需产品决策**）

### 5.1 单标签专业类结构性占优，以及医学五类无法区分（真问题）

**这是用户最初怀疑的「相关词差距太小」的真正所在**，而且是可以量化的。

93 个专业类里有 **8 个只挂 1 个产业标签**：

| 专业类 | 唯一标签 |
|---|---|
| 临床医学类 / 口腔医学类 / 医学技术类 / 中西医结合类 / 护理学类 | `医疗健康/临床` |
| 公安学类 | `政府/公共服务` |
| 动物生产类 | `农业/食品/林业` |
| 教育学类 | `教育培训` |

**单标签在公式上恒等于满分**：`industry_match = sqrt(best × mean)`，标签只有一个时
`best == mean == 该标签的比值`，于是 `sqrt(r × r) = r`——**完全不受均值项的稀释**。
多标签专业类只要有一条标签用户不感兴趣就被拉低，单标签的不会。

后果有两个：

1. **医学五个类对任何用户都永远一模一样。** 它们的标签集合都是 `{医疗健康/临床}`，
   一个医学意向用户拿到的契合度是 **5 个并列的 1.000**，谁在前谁在后只能由排序规则决定。
   而临床医学（当医生）、口腔医学（当牙医）、护理学、医学技术（检验/影像）、
   中西医结合，**是五个出路完全不同的专业**——一个标签抹平了它们。
2. **同门类姊妹专业类的名次由「标签打了几个」而不是「专业是什么」决定。**
   农业意向用户的实测排序：`动物生产类`（1 个标签）`契合度 1.000` 排第 1，
   `植物生产类`（2 个标签：农业 + 科研/学术）`契合度 0.802` 排第 **10**。

**纠正**：上一版本报告把这件事写成「植物生产类接不住农业意向」，**这个说法是错的**。
实测农业意向用户的 Top8 是 `动物生产类 / 草学类 / 农业工程类 / 化工与制药类 /
生物工程类 / 海洋科学类 / 林学类 / 自然保护与环境生态类`——**全部是农业与环境方向**，
系统对农业意向的服务是正常的。`植物生产类` 掉到第 10 名也不是打分错误：
它的第二条标签 `科研/学术` 该用户确实只有 28.6 分（满分 100），
`sqrt(1.0 × 0.643) = 0.802` 是对「两条出路里只有一条对口」的如实反映。

**所以真正的问题不是公式，是标签**：同门类姊妹专业类的标签粒度不一致
（8 个门类里，农学 7 个类的标签数是 1/2/3 三种），名次差异来自标注差异而非专业差异。

**没有动标签**：给 93 个专业类重新标注是专业判断题（临床 vs 护理 vs 医技该怎么区分，
需要医学领域知识），且会全局改变所有用户的推荐，不是我该单方面决定的。
建议按下面 5.3 的清单人工过一遍。

### 5.2 `SCORE_ALIGNMENT_MATRIX` 单元格碰撞——**实测影响可忽略，不改**

矩阵里有两处取值碰撞：`top` 档 `极高 == 高 == 1.0`；`upper` 档 `高 == 中 == 0.9`。

**纠正**：上一版把这里写成「并列的第二个来源（占比 16%）」，**这个说法是错的**。
两个原因：

1. **用户看到的那个数字根本不经过矩阵**。卡片上显示的是 `fit_score`，
   而 `fit_score = industry_match`（`funnel_engine.py:1173`），只由产业标签决定。
   矩阵只进入隐藏的排序分 `category_score`。
2. **直接测量过了**：把矩阵的碰撞格拉开（保持每格相对大小关系，只把「恰好相等」
   变成「不等」），在 600 份同一批答卷上重跑——
   截断线（第 8 vs 第 9 名同分）并列从 **24 份降到 23 份**，即矩阵碰撞直接造成的
   只有 **5 / 600 ≈ 0.8%** 的答卷。

而且改动它需要一个我拿不到的前提：`top` 档三档等同是否有意为之（例如
「位次足够高时，专业热门程度不再影响判定」——这是一种合理的饱和设计）。
**没有证据支持它是 bug，改动会全局重标定一张手工矩阵，所以不动。**
（即便它对排序有影响，`rank_by_score` 也已经把并列的次序变成确定的了。）

### 5.3 L1「学科门类」完全不看用户申报的产业意向（观察，未改）

`layer1_discipline_match` 的打分只用 `micro_behavior_vector`（30 道微观题）与
`inferred_personality`，**源码里没有任何 `macro_industry_vector` 的引用**——
也就是说 M1–M11 里明确申报的产业方向，在门类层完全不参与。

门类分再以 `×(0.75 + 0.50 × disc_score)`（约 ±25% 的摆动）传导进 Layer 2。

实测两个真实画像：

| 画像 | L1 第 1 名 | 医学 vs 工学的门类乘数差 |
|---|---|---|
| 技术/编程 | 工学 0.808 | 工学高出 **+7.0%** |
| 医学/临床 | 医学 0.747（工学 0.742 紧随） | 工学低 0.2% |

技术画像没问题。医学画像下**医学与工学在门类层只差 0.005**，门类层几乎是
「由行为画像单独决定」，而该用户在产业题里申报的是 `医疗健康/临床 = 100.0`。

**没有改**。当前两个画像下结论都对，说明它不是正在发作的缺陷，而是一个**风险**：
如果某个用户的行为画像与申报方向冲突（比如动手能力强、却明确要学医），
门类乘数的 ±25% 摆动会压过 L2 上的契合度差。要不要把产业意向接入 L1、
或者把传导幅度调小，属于设计取舍，需要你定。

### 5.4 M11 单题申报敌不过 M1–M10 的十题（**v6.3 已解决**，见版本报告 v6.3）

β=0.5 改善了物流/体育的接住率，但**物流管理与工程类仍然进不了 Top8**。
根本矛盾没变：M11 只能提供 1 题的证据量，M1–M10 提供 10 题。
彻底解决需要把 M11 升级为「**明确申报方向**」，在排序上单独给权重，
而不是和 M1–M10 混在同一个向量里。这属于问卷结构改动，需要产品决策。

---

## 六、回归测试

新增 4 组 13 断言，锁住本版的行为，防止将来退化：

| 测试 | 锁的是什么 | 结果 |
|---|---|---|
| Test 10 中性选项 | 老答卷零影响 / 全中性→产业向量为 0 / 风险容忍度不误判 | 4 断言 PASS |
| Test 11 键顺序无关 | 打乱 JSON 键顺序后 Top8 名单不变 | PASS（0/160） |
| Test 12 并列披露 | `fit_tied_with` 存在、两两对称、显示值确实相同 | 4 断言 PASS |
| Test 13 未申报打折 | 全申报=等权 / 未申报越多惩罚越轻 / 全未申报仍为 0 / 未申报比申报不搭扣分少 | 4 断言 PASS |

全量：`test_algorithm_quality.py` 13 组 **67 断言全部通过**；`test_funnel.py` 3/3 通过。

---

## 七、部署状态

`github_upload/` 已同步，逐文件 md5 校验一致（见第八节校验输出）。

安全检查：`github_upload/` 中**无** `.streamlit/secrets.toml`、**无**旧数据文件、
**无** `__pycache__` / `*.pyc`；本版新增的临时量测脚本已全部删除，未进入 upload。
