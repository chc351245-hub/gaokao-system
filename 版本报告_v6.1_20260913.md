# 高考志愿智能推荐系统 — 版本报告

**版本**：v6.1（在 v6.0 三层漏斗引擎基础上的结构性修订）
**日期**：2026-09-13
**引擎**：`funnel_engine.py`（三层漏斗 v6.0 核心 + 本版修订）
**状态**：全部回归测试通过（54 断言 + test_funnel 3/3 + test_recommendation），`github_upload/` 已同步并逐文件 md5 校验一致

> 说明：本报告按**代码实际实现**撰写。仓库里另一份 `高考志愿推荐系统_技术报告_v6.0.docx`
> 由 `generate_report.py` 生成，该脚本仍描述旧版「Jaccard 指数」产业匹配（代码中已废弃），
> **不可直接用于对外说明**，需另行修订。本报告不依赖该脚本。

---

## 一、本版修订概要

本版解决两个用户可感知的问题，并修掉一个既有缺陷：

| # | 问题 | 改动 | 实测效果 |
|---|---|---|---|
| 1 | 推荐第一有时是「物流管理与工程类」等不合常理的专业类 | 删除「33 标签 → 10 集群」映射层，用户向量与专业类标签统一为 33 维词表 | 物流霸榜的成因消失；Top8 平均不同分值 6.92 → **7.82 / 8** |
| 2 | 推荐分差太小 | 同上（词表粒度决定区分度上限） | 契合度极差 0.359；契合度为 0 的专业类占比 **0%** |
| 3 | 推荐文案「太官方、重复率高」 | 重写推荐理由生成 + 标签分层 | 每页结构重复 0.26 → **0.04** 条 |

---

## 二、改动一：产业词表 10 维 → 33 维（结构性）

### 2.1 病因

原实现在**用户向量**（10 个产业集群）和**专业类标签**（33 个细粒度产业）之间，
靠一张 `INDUSTRY_TAG_TO_CLUSTER` 做多对一压缩。这张表是两处长期缺陷的共同根源：

- 「政府公共」一个集群吞并了 `法律服务/合规`、`科研/学术`、`军事/国防工业`、`房地产/物业`，
  覆盖 **50/93 = 53.8%** 的专业类，几乎不携带区分信息。
- 「智能制造」吞并了 `物流/供应链`、`建筑/土木/城规`、`航空航天`、`海洋工程/船舶`，
  使「物流管理与工程类」凭空继承了两个最热门的集群，而它真实的去向
  （`物流/供应链`）**从头到尾没有参与过打分**。实测 300 份随机答卷中，
  16 份把它推为第一名、92 份进 Top8。

同时，10 维粒度下**一批专业类的产业画像完全相同**（纯生物医药类一律 1.000），
契合度必然并列——这就是「分差小」的根源。

### 2.2 一个被实测否掉的方案

曾设想「爬取更多关键词来重新加权」。实测结论：**无效**。对现有 10 个集群做任何
重新加权（IDF 加权、稀有度折损），结果几乎不动分毫，物流第一的频率甚至从
10/200 升到 14/200。信息是在**映射那一步**被销毁的，不是打分环节。

### 2.3 本版做法

用户向量与专业类标签**共用同一套 33 维词表**（`user_profile.INDUSTRY_DIMENSIONS`），
映射层被**彻底删除**：

- `INDUSTRY_TAG_TO_CLUSTER`、`UNMAPPED_INDUSTRY_TAGS`、一致性守卫 —— 全部移除
- `layer2_category_match` 直接用专业类的 `industry_map` 标签查用户向量，无需任何转换
- 问卷 `M1–M10` 的产业权重按选项原文重新落到 33 个标签上
- **新增 M11**（8 选 1），专门覆盖原来整个问卷都表达不了的方向：
  建筑/物流/航空航天/农业/能源/体育

### 2.4 一致性验证

- 33 个维度**全部**被至少一个专业类使用，无冗余维度
- 没有任何专业类使用超出这 33 个的标签
- 词表完全对齐，无死角

### 2.5 实测效果（400 份随机答卷）

| 指标 | 修订前 | 修订后 |
|---|---|---|
| Top8 平均不同分值 | 6.52 / 8 | **7.82 / 8** |
| 契合度极差 | — | 0.359 |
| 契合度为 0 的专业类 | — | **0%** |

四类主流画像的第一名：技术/编程 → 电子信息类；传媒/创作 → 中国语言文学类；
医学/临床 → 医学技术类；金融/商业 → 统计学类。

---

## 三、改动二：推荐文案重写

### 3.1 病因（实测）

120 份答卷共 **4720 条**专业推荐理由，句式只有 **3 种**：

| 句式 | 占比 |
|---|---|
| 「你的微观行为模式（…）与此专业核心要求极度吻合」 | 2.4% |
| 「你在（…）等方面与此专业有较好匹配」 | 30.0% |
| 「**你的核心优势**（抗压能力(59分)、创造性思维(48分)）与此专业有一定关联」 | **67.6%** |

问题不在文采，在于**占比三分之二的第三档根本不提专业**——它只夸用户，括号里填的是
用户自己的维度分。「抗压能力(59分) 与此专业有一定关联」这句话，套在哲学、护理、
土木上全都成立。真正提到专业的那档门槛是 `micro >= 0.85`，只有 2.4% 够得着。

**结论**：这不是「词汇贫乏」，靠爬取网络评价解决不了；是三分之二的理由没说这个专业要什么。

### 3.2 本版做法

三档全部重写为「必须提到该专业的核心动作」。以占比最大的第三档为例：

```
旧：你的核心优势（抗压能力(59分)、创造性思维(48分)）与此专业有一定关联
新：工程审计要的是审计底稿编制、财务报表编制与分析，不是你最强的动手实验(58分)
    ——它上榜靠的是所属专业类的整体匹配
```

句式轮换按**名次取模**实现，而非哈希：

- 哈希（含 crc32）全局分布均匀，但**同一页内部不保证散开**——实测一页 6 个专业会挑出
  3 个同款句式。按名次取模保证相邻两个专业必然换一种说法。
- 不能用内置 `hash()`：它对字符串按进程随机加盐，同一专业刷新页面就会换一句话。
- 三个变体中只有**一个**点名用户的最强维度。该维度对同一用户是固定的，若每条理由都念，
  一页 4 个专业会出现 4 次「不是你最强的动手实验(58分)」。

### 3.3 实测效果

| 指标 | 修订前 | 修订后 |
|---|---|---|
| 每页结构重复 | 0.26 条（最差 3） | **0.04 条**（最差 2） |
| 每页点名用户同一维度 | 3.68 条 | **1.02 条** |
| 理由平均长度 / 最长 | — | 60 字 / 96 字（卡片截断 110，无截断） |

---

## 四、改动三：标签分层 + 接入三个此前未被使用的字段

### 4.1 标签分层

`[产业风口]`、`[高分敏感]`、`[低资源友好]`、`[薪资潜力高]` 四个标签的来源都是
**专业类**属性（`cat_labels`），挂在每张专业卡片上会让一页 6 张卡片重复同一个标签 6 遍。

现拆为两层：

- **专业类级别**（显示一次）：`[产业风口] [高分敏感] [低资源友好] [薪资潜力高]`
- **专业卡片级别**（只留专业之间会不同的）：`[微观契合] [招生体量极小] [极度内卷] [热度上升]`

每张卡片现在 0–3 个标签。

### 4.2 接入「采了没用」的字段

| 字段 | 原先状态 | 现状态 |
|---|---|---|
| `salary_potential` | 数据中存在，**全引擎 0 次引用** | 接入专业类级别标签 `[薪资潜力高]` |
| `enrollment_volume` | 仅作打分乘数，用户看不到 | 带进结果，进入正文与 `[招生体量极小]` 标签 |
| `hard_threshold` | 仅用于过滤 | 推荐中的专业会说明体检门槛（「你已符合」） |

---

## 五、顺带修复的既有缺陷

**`HOT_INDUSTRY_TAGS` 含两个永不命中的死标签**（`"新能源"`、`"碳中和"`，旧 10 集群词表残留）。
该缺陷在本版改动**之前就已存在**（已在改动前备份中确认），非本版引入。

修法是**删而不补**。实测：若改成新词表的 `能源/电力/碳中和` + `环保/新能源`，
`[产业风口]` 会从 30/93 涨到 43/93，新增的是草学类、林学类、自然保护与环境生态类、
海洋科学类、化学类这类——标成「风口」是误导。而真正该标风口的（电气类、能源动力类、
材料类、机械类）本来就靠其他标签命中了，删掉不会漏。

另修正卡片里 `{reason[:100]}...` 的省略号无条件拼接问题（短理由后面也会挂「...」，
看着像被截断）。

---

## 六、已知遗留问题（未解决，需产品决策）

本版解决了「分差小」与「物流霸榜」，但同时把更根本的矛盾顶到了台面上：

### 6.1 M1–M10 没有中性选项

每题都注入产业权重。一个对主流方向无感的用户，会因为反复选择「稳定/理论/规律」类选项
而**被动累积** `医疗健康/临床`（各题 0.1~0.15）与 `政府/公共服务`，最终峰值落在医学——
而他从未表达过医学意向。

### 6.2 仅由 M11 覆盖的方向接不住

只由 M11 覆盖的维度（物流/供应链、农业/食品/林业、体育产业）拿的是**1 题的证据量**，
无法对抗 M1–M10 的 10 题。实测：M11 选「物流」的用户，Top5 是医学技术类/中西医结合类/
法医学类/中医学类/交通运输类，**物流管理与工程类不进 Top8**；农业、体育同样接不住。
（建筑/航空航天/能源能接住，因为 M1–M10 中有对应选项协同。）

### 6.3 聚合方式对窄口径申报不利

`industry_match = sqrt(best × mean)` 会因「专业类挂着用户没点的标签」而扣分，而窄口径的
明确申报恰恰表现为「只有一条标签对得上」。医学技术类标签全是医疗 → mean≈1.0 胜出；
物流管理与工程类 {物流 1.0, 零售 0.35, 智能制造 0} → `sqrt(1.0×0.45)=0.67` 落败。
`mean` 项是防止「标签广度刷分」和提供区分度的关键，**不能直接删除**，需要换一种拆法。

### 6.4 关于「爬取网络大众评价」

不建议作为下一步。理由：

1. **不对症**——真问题是「67.6% 的理由不提专业」，爬评价不解决模板问题（该问题已由本版修复）。
2. **违反白皮书约束**——白皮书明确「契合度分只能衡量人↔专业的内在匹配，录取概率必须
   另立维度，不得污染契合度」。大众评价是**外部口碑**，一旦进入 `score` 即违反该约束。
3. **口碑有强劝退偏向**——生化环材、土木、新闻在网上是压倒性负面，不过滤会带偏推荐；
   且搬运知乎/贴吧的 UGC 存在 ToS 与版权风险。

若将来确实需要，只能做成**独立的展示维度**（如「网上怎么说」），绝不参与打分。

---

## 七、部署状态

`github_upload/` 已与主目录同步，逐文件 md5 校验一致：

```
一致  app.py                    一致  questionnaire.py        一致  test_funnel.py
一致  funnel_engine.py          一致  user_profile.py         一致  test_algorithm_quality.py
一致  generate_report.py        一致  recommendation_engine.py 一致  test_recommendation.py
一致  requirements.txt          一致  gaokao_majors.xlsx        一致  算法设计白皮书.md
一致  label_checkpoints/ 下 4 个数据文件全部一致
```

安全检查：`github_upload/` 中**无** `.streamlit/secrets.toml`（含 Supabase 密钥与管理员
密码，线上配置在 Streamlit Cloud 的 Settings → Secrets）；**无** `enhanced_majors.json`
旧数据；**无** `__pycache__` / `*.pyc`。

---

## 八、回归测试

| 测试 | 结果 |
|---|---|
| `test_algorithm_quality.py` | 9 组 54 断言 **全部通过** |
| `test_funnel.py` | 3 组 **全部通过** |
| `test_recommendation.py` | **全部通过**（覆盖已废弃的 v5.0 引擎） |
