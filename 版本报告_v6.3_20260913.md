# 高考志愿智能推荐系统 — 版本报告 v6.3

**日期**：2026-09-13
**上一版**：v6.2（同日）
**本版主题**：把 M11 从「第十一题」升格为「明确申报方向」并单独提权，
以及申报方向没进榜时的「特别提示」块

---

## 一、本版起因

v6.2 的第五节留了一条未决问题：

> ### 5.4 M11 单题申报敌不过 M1–M10 的十题
> β=0.5 改善了物流/体育的接住率，但**物流管理与工程类仍然进不了 Top8**。
> 根本矛盾没变：M11 只能提供 1 题的证据量，M1–M10 提供 10 题。
> 彻底解决需要把 M11 升级为「明确申报方向」，在排序上单独给权重。

本版就是做这件事。

补充说明为什么要单独拎出来：M11 是全问卷**唯一**一道措辞是
「如果必须选一个更具体的行业方向，你更愿意去?」的题。M1–M10 问的是经历、
性格、价值观，产业方向是从答案里**推测**出来的；M11 是用户**直接选**的。
把两者混进同一个 `macro_industry_vector` 求平均，等于让 10 题的推测
去稀释 1 题的明说。这不是权重调参能修的，是结构问题。

---

## 二、改动内容

### 2.1 数据结构：新增 `declared_industry_vector`

`user_profile.py` 的 `UserProfile` 增加字段，**只由 M11 填充**，且**不归一化**
（保留权重原值）——归一化会把「用户申报了几条出路」这个信息抹掉，
而归一化后的向量也不再能回答「哪条是主方向」。

### 2.2 解析：`declared_industry_from_answers()`

`questionnaire.py` 新增函数，从答卷里取出 M11 选中的选项的 `industry_weights`：

| 作答 | 结果 |
|---|---|
| `{"M11": "B"}` | `{'物流/供应链': 0.55, '零售/电商/消费': 0.3, '互联网/软件': 0.15}` |
| `{"M11": "G"}`「以上都不是」 | `{}`（没有申报任何方向） |
| 没答 M11 | `{}`（不凭空安一个方向） |

### 2.3 提权：`DECLARED_BOOST = 0.35`

在 `layer2_category_match` 里，算完 `industry_match` 之后、特殊赛道之前插入：

```python
declared = user.declared_industry_vector or {}
if declared:
    max_declared = max(declared.values())
    if max_declared > 0:
        covered = sum(w for tag, w in declared.items() if tag in ind_dims)
        coverage = min(1.0, covered / max_declared)
        if coverage > 0:
            industry_match += (1.0 - industry_match) * DECLARED_BOOST * coverage
```

三个设计选择，各自都有理由：

**① 为什么用「拉拽式」`x + (1-x)·β·coverage` 而不是硬地板 `max(x, 0.65)`。**
特殊赛道（医学/师范/军警）用的就是硬地板，但硬地板会把所有命中的专业类
一起顶到同一个数——那正是 v6.2 花整版力气消灭的**并列来源**。拉拽式保序、
连续、上界仍是 1.0。

**② 为什么分母是「申报里最大的那条权重」而不是「权重之和」。**
要回答的是「这个专业类服务的是我申报的**主方向**，还是顺带的次要方向」。
M11 的 C 选项是 `{航空航天 0.4, 海洋工程/船舶 0.3, 军事/国防工业 0.2,
智能制造/机器人 0.1}`：航空航天类命中 0.4/0.4 = 1.0，拿满提权；只命中船舶的
拿 0.75。用「权重之和」做分母则两者都会被判成「覆盖得很全」。

**③ 为什么 β 是扫出来的。** 扫描 0.0 / 0.15 / 0.25 / 0.35 / 0.50 / 0.70，
指标是「M11 明确申报某方向时，代表专业类进 Top8 的份数（40 份/场景）」，
同时监督聚合质量不能变差（40 份/场景，4 类主流画像随机取底）：

| β | 物流 | 建筑 | 航空航天 | 农业* | 不同分值 | 极差 | 并列答卷 |
|---|---|---|---|---|---|---|---|
| 0.00 | 22 | 23 | 12 | 0.8 | 7.540 | 0.4343 | 41.0% |
| 0.15 | 22 | 23 | 12 | — | — | — |
| 0.25 | 22 | 32 | 12 | 0.8 | 7.513 | 0.4409 | 40.8% |
| **0.35** | **40** | 29 | **25** | **1.2** | **7.550** | 0.4221 | **41.0%** |
| 0.50 | 40 | 29 | 25 | 1.8 | 7.530 | 0.4091 | 42.5% |
| 0.70 | 40 | 29 | 32 | 2.4 | 7.465 | 0.3766 | 50.0% |

取 **0.35**：物流 22→40（全中）、航空航天 12→25，而**聚合质量一分没掉**
（不同分值 7.540→7.550，并列答卷 41.0% 持平）。0.50 命中数与 0.35 完全相同，
并列却涨到 42.5%；0.70 多换到航空航天 25→32，代价是并列涨到 50.0%、
极差从 0.43 掉到 0.38。四类主流画像的 #1 在**所有** β 下都保持正确。

\* β=0.15 的命中数与 0.00 **逐项完全相同**（22/23/12/0.8），太弱、等于没改，
所以当天没有再花钱去跑它的质量指标，那三格记 `—` 而不是补一个没测过的数。

\* 农业那一列在所有 β 下都是 0，**不是没修好**：`植物生产类` 多挂了一条
`科研/学术`，同类里还有 8 个专业类都挂着 `农业/食品/林业` 且契合度更高，
Top8 装不下。该看的指标是「该方向进榜的专业类个数」：0.8 → 1.2 → 2.4。

---

## 三、实测效果

### 3.1 提权确实改变了结果

同一张答卷、只切换 `DECLARED_BOOST`，主流 IT 画像（位次前 12/18/25%）：

| 申报方向 | 代表专业类 | 关提权 | 开提权 |
|---|---|---|---|
| B 物流 | 物流管理与工程类 | 0/3 进榜 | **3/3 进榜** |
| A 建筑 | 建筑类 | 0/3 进榜 | **3/3 进榜** |
| C 航空航天 | 航空航天类 | 0/3 进榜 | 0/3 进榜 |
| D 农业 / F 体育 | 植物生产类 / 体育学类 | 0/3 进榜 | 0/3 进榜 |

### 3.2 提权的作用范围是干净的

同一张答卷（申报 B），`DECLARED_BOOST` 0 → 0.35：

- 全表 89 个专业类里 **21 个被改动**
- 这 21 个**全部**挂着申报的三个标签之一（`物流/供应链` / `零售/电商/消费` /
  `互联网/软件`），一条都不挂的 **0 个**
- 21 个里**全部只升不降**，降的 0 个
- 契合度最高 1.0 上界未被突破

**注意口径**：这里必须和「同一张答卷关掉提权」比，**不能**拿 `M11=B` 和
`M11=G` 两张答卷比——M11 的权重同时也进 `macro_industry_vector`，两张答卷
底子本来就不同，差值里混着基础效应。第一版测试就是这么写的，得出了错误的
「提权把物流类从 0.50 抬到 0.91」的结论，实际是 0.864 → 0.912。

---

## 四、申报方向没进榜怎么办：「特别提示」位

提权只抬高 `industry_match`，也就是**只抬高契合度**。Top8 的座次由
`category_score` 决定：

```
category_score = industry_match×0.5 + asset_match×0.2 + score_match×0.3
                 → 再乘 L1 学科门类得分与特殊赛道系数
```

提权只吃到其中一半权重。所以上表里 C 航空航天类虽然契合度确实涨了，
座次仍进不了 Top8——它的 `asset_match` / `score_match` 和所属学科门类
仍按本人的实际情况算。**这符合白皮书**（契合度只衡量人↔专业的内在匹配，
不得掺入录取维度），但用户的心理预期是「我明说了就该给我看」。

这是产品取舍，没有擅自改。**你拍板选了折中方案：名单不动，另起一块如实交代。**
本版按这个方案实现。

### 4.1 实现

新增 `funnel_engine.declared_direction_note(user, shown_names)`，在推荐名单
**之前**渲染一个提示块。它**不参与任何排序、不进任何公式**。

```
📣 你明确申报的方向，有专业类没能进入下面的名单

🔸 你申报的「农业/食品/林业」有 1 个专业类进了上面的名单（海洋科学类），
   另有 14 个相关专业类没进（下面列出其中最接近的 2 个）。
   - 生物工程类：契合度 0.673（全表第 28 名），说明你与它确实对口；
     但综合排序分 0.935 只排第 9 名——差距出在分数位次匹配（0.90）
   - 草学类：契合度 0.955（全表第 1 名），说明你与它确实对口；
     但综合排序分 0.896 只排第 18 名——差距出在分数位次匹配（0.40）

🔸 你申报的「环保/新能源」有 3 个专业类进了上面的名单（…），另有 6 个没进。
   - 化工与制药类：综合排序分其实排在第 6，在上面这 8 个之内，
     是被「同一学科门类不超过 4 个」的多样性规则挤下去的
```

### 4.2 三个必须处理的细节

**① 落榜原因有两种，不能混为一谈。** 一种是**分数不够**（综合排序分排在
第 9 名开外）；另一种是**分数够了、被多样性打散换掉的**（`_run_funnel_impl`
在 L2 选出前 8 之后，会把同一学科门类超过 4 个的末位替换成跨门类候选）。
后者如果只说「综合排序分 0.894」，用户会以为分数不行，实际是主动让位。
所以提示块按 `rank <= 名单长度` 分流，分别给两套说法。

**②「契合度低」时不许说「对口」。** 第一版无论高低都套同一句式，于是印出
「**政治学类：契合度 0.197（全表第 39 名）说明你与它确实对口**」——一句假话。
现在只有 `fit >= 0.6` 才说对口，否则如实说「这个方向和你的画像本来就只沾一点边」。
回归测试专门锁了这一条。

**③ 输出必须短。** 一个申报方向底下可能挂十几个专业类（`农业/食品/林业`
有 14 个落榜）。全列出来不是「提示」是刷屏。现在最多 2 个方向 × 2 个代表，
且**跨方向去重**（`化工与制药类` 同时挂着 `环保/新能源` 和 `制药/生物技术`，
同页只出现一次），其余用「另有 N 个」交代个数并注明「下面列出其中最接近的 N 个」，
不假装只有这些。

### 4.3 性能

提示块在**跑漏斗时**算好存进 `session_state.declared_notes`，不在渲染函数里现算。
`render_funnel_results` 是挂在 session_state 上、每次交互都会重跑的——现算等于
用户每展开一个折叠卡片就把 Layer 1 + Layer 2 全量重跑一遍（93 个专业类）。
清空结果的两个分支（「返回修改答案」「重新开始」）也一并清了 `declared_notes`。

---

## 五、回归测试

新增 Test 14「M11 明确申报方向单独提权」（9 条断言）与 Test 15
「M11 申报落榜提示」（15 条断言）：

### Test 15 锁的是什么

提示块的价值全在「它说的话是真的」——一个把「契合度 0.197」说成「说明你与它
确实对口」的提示，比没有提示更糟。所以 Test 15 主要不是测功能，是测**措辞与
事实一致**：

| 断言 | 锁的是什么 |
|---|---|
| 不申报 → 不产生提示 | M11 选「以上都不是」时不该冒出一块 |
| 生成提示前后 Top8 不变 | 提示块**没有**参与排序（防止后人顺手把它加进去） |
| ≤2 个方向、每方向 ≤2 条 | 输出不会退化成刷屏 |
| 跨方向不重复；列出的确实没进榜；`in_top` 的确实进了榜 | 内容与名单一致 |
| `headline` 说「没有任何专业类」时 `in_top` 必须真的为空 | 最严重的一句话不能误报 |
| 契合度 <0.6 的**不说**「确实对口」 | 锁住上面那个假话 bug |
| 归因到「多样性」的，综合排序分必须真的在名单长度之内 | 归因不能张冠李戴 |
| 名单不满 8 个时不许归因到多样性 | 保险丝分支 |

「归因到多样性」那条最初拿 M11=F 画像测，因为该画像的落榜类全是「分数本来就
不够」，**以空列表通过，等于没测**。换成 M11=B 画像（`交通运输类` 确实是
综合排序分第 5 却被多样性换掉的）才真正有了断言对象，并额外加了一条
「B 画像下确实产生了多样性归因」来防止它再次退化成空测。

### Test 14 断言清单

| 断言 | 锁的是什么 |
|---|---|
| 解析 B / 解析 G / 没答 M11 | 申报语义正确，不会凭空安方向 |
| IT 画像申报 B、A → 0/3 变 3/3 | 提权确实改变结果（带对照组） |
| 提权只作用于挂着申报标签的专业类 | 21 个被改动，越界 0 个 |
| 被提权的类只升不降 | 21 个里降的 0 个 |
| 契合度上界仍是 1.0 | 越界 0 个 |

测试用 `FE.DECLARED_BOOST` 动态读取常量并 `try/finally` 复原，改 β 时测试
不用跟着改，也不会污染后续测试。

全量：`test_algorithm_quality.py` **15 组 91 断言全部通过**；
`test_funnel.py` 3/3 通过。

---

## 六、部署状态

`github_upload/` 已同步（`app.py`、`funnel_engine.py`、`questionnaire.py`、
`user_profile.py`、`test_algorithm_quality.py`、本报告、v6.2 报告勘误），
逐文件 md5 校验一致。

安全检查：`github_upload/` 中**无** `.streamlit/secrets.toml`、**无**旧数据文件、
**无** `__pycache__` / `*.pyc`；本版新增的临时量测脚本（`_exp_harness.py` /
`_exp_sweep.py` / 输出 txt）已全部删除，未进入 upload。
